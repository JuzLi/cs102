GenerateDatabase.sql is used to generate the database used for this project.

To change the name of the database, replace "Create Schema cs102" with "Create Schema <Database Name>" and 
"use cs102" with "use <Database Name>"

Take note of the username, password, and server that your SQL server is hosted on.

Your username and server can be found by pressing Database > Connect to Database